function romanEncoder(roman) {
    //make the object to contail all roman numerals and their values
    special = {
        'I': 1,
        'V': 5,
        'X': 10,
        'L': 50,
        'C': 100,
        'D': 500,
        'M': 1000,
    }
    str = new String(roman).split('').map((e, i) => {});


}